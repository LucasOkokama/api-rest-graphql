# Ignore node_modules (standard for JS/TS projects)

node_modules/

# Ignore common build output directories

dist/
build/
out/
.next/ # Next.js build folder
.angular/ # Angular build cache
.esbuild/ # esbuild output/cache
.cache/ # Vite, Nuxt, or other tooling caches
.vite/ # Vite-specific cache directory

# Ignore lock files (optional – depends on your team's strategy)

package-lock.json
pnpm-lock.yaml
yarn.lock

# Ignore OS and IDE-specific files

.DS_Store # macOS
Thumbs.db # Windows
_.log # Log files
_.tmp # Temp files

# Ignore VSCode workspace settings (optional)

.vscode/

# Ignore files generated by testing or storybook

coverage/ # Code coverage reports
storybook-static/ # Storybook static build
